Навчальний проєкт “QA Automation Algorithms & Utilities Toolkit”, який прокачує алгоритмічне мислення, Python‑інженерію та навички Automation QA. 
Він підготовленний АІ як структурований набір задач — від простих до складних — у кожному розділі. 
Це дасть змогу рухатися поступово від простого до складного.

Проєкт обєднує набір корисних утиліт, які реально можуть знадобитися в автоматизації.

## Проєкт: QA Automation Toolkit у якому реалізовані:

    алгоритми,
    утиліти для тестування,
    API‑клієнт,
    логування,
    ретраї,
    кешування,
    структури даних,
    тести (pytest),
    документацію.

🔥 Структура проєкту

    qa_toolkit/
        algorithms/
        utils/
        api/
        data_processing/
        tests/

## 🟦 Розділ 1. Базові алгоритмічні задачі (від простого до складного)
### 1.1. Простий рівень
#### 1.1.1 Написати функцію, яка перевіряє, чи є рядок паліндромом.
Написати функцію:
is_palindrome(text: str) -> bool

Умова:
- ігнорувати пробіли,
- ігнорувати регістр,
- ігнорувати розділові знаки (.,!?-).

Приклади:

    "Level" → True
     "A man, a plan, a canal: Panama" → True
    "Hello" → False

#### 1.1.2 Порахувати кількість голосних у рядку.
Написати функцію:
count_vowels(text: str) -> int

Умова:
- враховувати голосні: a, e, i, o, u, y
- регістр не має значення

Приклади:

    "Hello" → 2
    "Привіт" → 2
    "Automation" → 6

#### 1.1.3 Знайти максимум у списку без max().
Написати функцію:
find_max(numbers: list[int]) -> int

Умова:
- список може бути порожнім → повернути None
- не можна використовувати max()

Приклади:

    [1, 5, 3] → 5
    [-10, -3, -50] → -3
    [] → None

#### 1.1.4 Знайти мінімум у списку без min()
Написати функцію:
find_min(num_list: list[int]) -> int | None:

Вимоги:
 - якщо список не порожній → повернути найменше число;
 - якщо список порожній → повернути None (як ти зробив у find_max);
 - не використовувати min().

#### 1.1.5 Порахувати суму елементів без sum()
Реалізувати функцію sum_list(numbers), яка повертає суму всіх елементів списку без використання вбудованої функції sum().

Вимоги:
 - Не використовувати sum().
 - Функція має бути чистою — не змінювати вхідний список.
 - Додати type hints.
 - Додати докстрінг.
 - Якщо список порожній → повернути 0.
 - Обробити edge cases:
 - список з одним елементом
 - список з негативними числами
 - список з float
 - список з нулями

📌 Приклади:
```
sum_list([1, 2, 3]) → 6
sum_list([-1, -5, 10]) → 4
sum_list([10]) → 10
sum_list([]) → 0
sum_list([1.5, 2.5]) → 4.0
```

#### 1.1.6 Порахувати кількість парних чисел у списку

#### 1.1.7 Розвернути список без reverse() і без [::-1]
Реалізувати функцію reverse_list(numbers), яка приймає список чисел і повертає новий список, у якому елементи розташовані у зворотному порядку.

Вимоги:
 - Не використовувати reversed() або list.reverse().
 - Функція має бути чистою — не змінювати вхідний список.
 - Додати type hints.
 - Додати докстрінг.
 - Обробити edge cases:
   - порожній список
   - список з одним елементом
   - список з float
   - список з негативними числами

Приклад:
```
reverse_list([1, 2, 3]) → [3, 2, 1]
reverse_list([]) → []
reverse_list([10]) → [10]
reverse_list([-5, -1, -10]) → [-10, -1, -5]
```

#### 1.1.8 Перевірити, чи всі елементи у списку унікальні

#### 1.1.9 Знайти другий найбільший елемент у списку
Без sort() і без max().

#### 1.1.10 Порахувати кількість входжень кожного елемента

#### 1.1.11 Об’єднати два списки в один без + і без extend()
Тут перевіряється робота з циклами.

### 1.2. Середній рівень
Перевірити, чи два рядки — анаграми.

Реалізувати binary search.

Повернути список унікальних елементів, зберігаючи порядок.

### 1.3. Складний рівень
Розвернути однозв’язний список.

Перевірити баланс дужок ()[]{}.

Реалізувати LRU Cache.

## 🟩 Розділ 2. Задачі для Automation QA
### 2.1. Простий рівень
Парсинг логів: знайти кількість рядків з "ERROR".

Валідація структури JSON (перевірити наявність ключів).

### 2.2. Середній рівень
Реалізувати retry‑декоратор.

Реалізувати простий аналог WebDriverWait: чекати появи умови з таймаутом.

Генератор унікальних email/username.

### 2.3. Складний рівень
Мок HTTP‑запитів (імітація API).

Порівняння двох JSON з ігноруванням порядку ключів.

Перевірка списку URL на доступність (200/301/404).

## 🟧 Розділ 3. Структури даних
### 3.1. Простий рівень
Реалізувати стек.

Реалізувати чергу.

### 3.2. Середній рівень
Черга на двох стеках.

Пошук топ‑N найчастіших слів у тексті.

### 3.3. Складний рівень
Реалізувати граф (adjacency list).

BFS та DFS.

Пошук найкоротшого шляху (Dijkstra).

## 🟥 Розділ 4. API‑тестування та утиліти
### 4.1. Простий рівень
Функція, яка робить GET‑запит і повертає статус‑код.

Перевірка JSON‑схеми (мінімальна).

### 4.2. Середній рівень
Паралельна перевірка списку URL.

Кешування результатів API‑викликів.

### 4.3. Складний рівень
Написати свій маленький API‑клієнт (наприклад, для OpenWeather).

Додати логування, ретраї, таймаути.

Написати тести для цього клієнта.

## 🟪 Розділ 5. Оптимізація та чистий код
### 5.1. Простий рівень
Декоратор для логування часу виконання.

Функція з мемоізацією.

### 5.2. Середній рівень
Паралельне виконання задач через ThreadPoolExecutor.

Асинхронний збір даних з API.

### 5.3. Складний рівень
Реалізувати власний Task Scheduler (аналог cron).

Написати pipeline обробки даних (ETL).



Це повинно виглядати як набір інструментів Automation QA, який можна показати на співбесіді.
